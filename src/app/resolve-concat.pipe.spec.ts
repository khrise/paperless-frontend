import { ResolveConcatPipe } from './resolve-concat.pipe';

describe('ResolveConcatPipe', () => {
  it('create an instance', () => {
    const pipe = new ResolveConcatPipe();
    expect(pipe).toBeTruthy();
  });
});
